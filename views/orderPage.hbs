<!DOCTYPE html>
<html translate="no">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0,user-scalable=no">
    <title>Happy Home</title>
    {{>libs}}
</head>

<body>
    {{>nav}}
    <div class="main container">

        <div class="header-line col-md-12">
            <h4 class="text-center" style="font-size:xx-large">
                Sifariş
            </h4>
        </div>
        
        <div class="col-md-12" style="margin-top:50px" id="ordersContainer">
                <div class="col-md-12" style="padding:20px">
                    <div class="{{#if order.isDone}}done{{/if}} col-md-12 card" style="padding-top: 10px;">
                        <div class="col-md-12 float-left">
                            <p class="text-muted">Sifariş ID: {{order._id}}</p>
                            <p>Ad,Soyad: {{order.name}}</p>
                            <p>Telefon nömrəsi: {{order.phoneNum}}</p>
                            <p>E-mail: {{order.email}}</p>
                            {{#if order.address}}<p>Ünvan: {{order.address}}</p>{{/if}}
                            {{#if order.notes}}<p>Qeydlər: {{order.notes}}</p>{{/if}}
                        </div>
                        <div class="col-md-12 float-left" style="margin-top:10px">
                            <p class="" style="font-size: x-large;">Sifarişlər</p>
                        </div>
                        {{#each order.orders}}
                        <div class="col-md-12" style="padding:20px;float:left">
                            <div class="card row"
                                style="padding:10px">
                                <div class="col-md-12 float-left">
                                    <img onclick="window.open('/product/{{this.itemID}}')" class="col-md-2" src="/data/pic/{{this.itemID}}">
                                </div>
                                <div class="col-md-12 float-left" style="padding-top:10px">
                                    <p class="text-muted">ID: {{this.itemID}}</p>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>

        {{>footer}}
    </div>


    {{>backToTop}}
    {{>whatsAppHotkey}}
</body>

</html>